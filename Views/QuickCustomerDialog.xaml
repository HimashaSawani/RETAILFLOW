<Window x:Class="RetailFlow.Views.QuickCustomerDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Register Loyalty Customer" 
        Height="360" Width="440"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F8FAFC"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style x:Key="DialogInputStyle" TargetType="TextBox">
            <Setter Property="Height" Value="40"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="12,8"/>
            <Setter Property="Background" Value="#FFFFFF"/>
            <Setter Property="BorderBrush" Value="#CBD5E1"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="8"/>
                </Style>
            </Style.Resources>
        </Style>

        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="8"/>
                </Style>
            </Style.Resources>
        </Style>

        <Style x:Key="CancelBtn" TargetType="Button">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Background" Value="#E2E8F0"/>
            <Setter Property="Foreground" Value="#475569"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="8"/>
                </Style>
            </Style.Resources>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,18">
            <TextBlock Text="⭐ NEW LOYALTY CUSTOMER" FontSize="18" FontWeight="Bold" Foreground="#1E293B"/>
            <TextBlock Text="Register customer to start accumulating loyalty points." FontSize="13" Foreground="#64748B" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Phone Number -->
        <StackPanel Grid.Row="1" Margin="0,0,0,14">
            <TextBlock Text="Phone Number *" FontWeight="SemiBold" FontSize="13" Foreground="#334155" Margin="0,0,0,6"/>
            <TextBox x:Name="PhoneTextBox" Style="{StaticResource DialogInputStyle}"/>
        </StackPanel>

        <!-- Customer Name -->
        <StackPanel Grid.Row="2" Margin="0,0,0,14">
            <TextBlock Text="Full Name *" FontWeight="SemiBold" FontSize="13" Foreground="#334155" Margin="0,0,0,6"/>
            <TextBox x:Name="NameTextBox" Style="{StaticResource DialogInputStyle}"/>
        </StackPanel>

        <!-- Error Text -->
        <TextBlock x:Name="ErrorText" Grid.Row="3" Text="" Foreground="#DC2626" FontWeight="SemiBold" FontSize="12" Margin="0,0,0,10"/>

        <!-- Action Buttons -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Cancel" Style="{StaticResource CancelBtn}" Click="Cancel_Click"/>
            <Button Grid.Column="2" Content="Save &amp; Select" Style="{StaticResource PrimaryBtn}" Click="Save_Click"/>
        </Grid>
    </Grid>
</Window>
