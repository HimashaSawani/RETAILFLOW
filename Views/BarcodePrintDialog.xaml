<Window x:Class="RetailFlow.Views.BarcodePrintDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Generate &amp; Print Barcode Labels" 
        Height="540" Width="520"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#F8FAFC"
        FontFamily="Segoe UI">

    <Window.Resources>
        <Style x:Key="PrimaryBtn" TargetType="Button">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="8"/>
                </Style>
            </Style.Resources>
        </Style>

        <Style x:Key="PresetQtyBtn" TargetType="Button">
            <Setter Property="Height" Value="32"/>
            <Setter Property="Padding" Value="12,0"/>
            <Setter Property="Margin" Value="4,0"/>
            <Setter Property="Background" Value="#E2E8F0"/>
            <Setter Property="Foreground" Value="#334155"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Style.Resources>
                <Style TargetType="Border">
                    <Setter Property="CornerRadius" Value="6"/>
                </Style>
            </Style.Resources>
        </Style>
    </Window.Resources>

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <StackPanel Grid.Row="0" Margin="0,0,0,16">
            <TextBlock Text="🏷️ SKU BARCODE LABEL GENERATOR" FontSize="18" FontWeight="Bold" Foreground="#1E293B"/>
            <TextBlock Text="Print professional Code-128 barcode stickers for retail items." FontSize="13" Foreground="#64748B" Margin="0,4,0,0"/>
        </StackPanel>

        <!-- Product Summary Header Card -->
        <Border Grid.Row="1" Background="#F1F5F9" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="8" Padding="14,10" Margin="0,0,0,16">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <StackPanel Grid.Column="0">
                    <TextBlock x:Name="ProductNameText" Text="Product Name" FontWeight="Bold" FontSize="15" Foreground="#0F172A"/>
                    <TextBlock x:Name="ProductSkuText" Text="SKU: P001 | Category: Beverages" FontSize="12" Foreground="#64748B" Margin="0,2,0,0"/>
                </StackPanel>
                <TextBlock x:Name="ProductPriceText" Grid.Column="1" Text="Rs. 0.00" FontWeight="Bold" FontSize="16" Foreground="#0F766E" VerticalAlignment="Center"/>
            </Grid>
        </Border>

        <!-- Sticker Live Preview Area -->
        <GroupBox Grid.Row="2" Header="Sticker Preview (Actual Dimensions)" Margin="0,0,0,16" Padding="12">
            <Border Background="#F8FAFC" BorderBrush="#E2E8F0" BorderThickness="1" CornerRadius="6" VerticalAlignment="Center" HorizontalAlignment="Center" Padding="10">
                <ContentControl x:Name="StickerPreviewContainer"/>
            </Border>
        </GroupBox>

        <!-- Quantity Selector -->
        <StackPanel Grid.Row="3" Margin="0,0,0,18">
            <TextBlock Text="Number of Labels to Print:" FontWeight="SemiBold" FontSize="13" Foreground="#334155" Margin="0,0,0,6"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="100"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBox x:Name="QuantityTextBox" Grid.Column="0" Text="1" Height="36" FontSize="15" FontWeight="Bold" HorizontalContentAlignment="Center" VerticalContentAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" VerticalAlignment="Center" Margin="10,0,0,0">
                    <Button Content="1" Style="{StaticResource PresetQtyBtn}" Click="PresetQty_Click"/>
                    <Button Content="5" Style="{StaticResource PresetQtyBtn}" Click="PresetQty_Click"/>
                    <Button Content="10" Style="{StaticResource PresetQtyBtn}" Click="PresetQty_Click"/>
                    <Button Content="25" Style="{StaticResource PresetQtyBtn}" Click="PresetQty_Click"/>
                    <Button x:Name="StockQtyBtn" Content="Match Stock Qty" Style="{StaticResource PresetQtyBtn}" Click="MatchStock_Click"/>
                </StackPanel>
            </Grid>
        </StackPanel>

        <!-- Action Buttons -->
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="12"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="Close" Background="#E2E8F0" Foreground="#475569" Height="42" FontWeight="SemiBold" BorderThickness="0" Cursor="Hand" Click="Close_Click">
                <Button.Resources>
                    <Style TargetType="Border">
                        <Setter Property="CornerRadius" Value="8"/>
                    </Style>
                </Button.Resources>
            </Button>

            <Button Grid.Column="2" Content="🖨️ Print Labels Now" Style="{StaticResource PrimaryBtn}" Click="Print_Click"/>
        </Grid>
    </Grid>
</Window>
